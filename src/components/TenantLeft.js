import React, { useState } from "react";
import { NavLink as RouterNavLink } from "react-router-dom";
//import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";

import {
  Collapse,
  //Container,
  Navbar,
  NavbarToggler,
//   NavbarBrand,
  Nav,
  NavItem,
  NavLink,
  Button,
  Container,
//   UncontrolledDropdown,
//   DropdownToggle,
//   DropdownMenu,
//   DropdownItem
} from "reactstrap";

//import { useAuth0 } from "../react-auth0-spa";

const TenantLeft = () => {
    //     const { loading, user } = useAuth0();
    
    //   if (loading || !user) {
    //     return <Loading />;
    //   }
    
      return (
          <React.Fragment>
              <div className="BigBox col-md-7 row-eq-height">
              </div>
            </React.Fragment>
      );
}

export default TenantLeft;